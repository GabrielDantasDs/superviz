
class Task {
    private name: string;
    private completed: boolean = false;

    constructor(name: string, completed: boolean) {
        this.name = name,
        this.completed = completed
    }
}